# App (frontend) screenshots

Drop the captured PNGs here with **exactly these filenames** — they are referenced
from the README's Deliverables section, so they render on GitHub once present.

| Filename | What it shows |
|---|---|
| `console.png` | Simulation console: rider + driver panels over the live Bengaluru map, "API connected" |
| `quotes-surge.png` | Upfront fare quotes for all tiers (Mini/Sedan/XL) with the surge badge + route |
| `matching-offer.png` | Live allocation: rider "Finding your driver…" + driver's incoming ride-offer card (Accept/Decline, countdown) |
| `driver-assigned.png` | Driver assigned: driver card (name, vehicle, plate, rating) + rider OTP |
| `trip.png` | Trip in progress: car moving along the route, live tracking |
| `receipt.png` | Fare receipt with breakdown (base + distance + time × surge) |

**Format:** PNG, ideally ≤ ~1600px wide. Add or rename freely — just keep the
README table and the embeds in sync.
